class MapItem {
  MapItem(
      {required this.name,
      required this.address,
      required this.location,
      required this.favorites});
  String name, address, location;
  bool favorites;
}
